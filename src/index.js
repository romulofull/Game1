import React from 'react';
import ReactDOM from 'react-dom/client';
import Juego from './Juego'; // Asegúrate de que el nombre del archivo coincida

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <Juego />
  </React.StrictMode>
);

